/** Default destination country when none is specified (pricing, sends, forms). */
export const DEFAULT_COUNTRY_CODE = "GLOBAL";

/** Fallback wallet display currency when no wallet record exists. */
export const DEFAULT_WALLET_CURRENCY = "USD";

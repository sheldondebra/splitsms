name: splitsms_flutter
description: Official SplitSMS Flutter/Dart SDK — SMS, OTP, and wallet (starter)
version: 1.0.0
homepage: https://www.splitsms.com/sdk

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.0

dev_dependencies:
  lints: ^4.0.0
